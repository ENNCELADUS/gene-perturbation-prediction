name = "domain_mapper"
description = "Read-only mapper for code, data, config, and documentation paths in the virtual-cell synthetic-lethality repository."
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Stay in read-only mapping mode.
Before answering, read the repository root AGENTS.md and docs/01-blueprint.md.
Trace real code, config, data, and documentation paths before making claims. The active code is the single `src` package: `src/data` (splits, records, caches, `src/data/prepare` CLIs), `src/model` (STATE/Tx1/ESM2 adapters, residual head), `src/training` (joint loop, sampling, checkpoints), `src/eval`, `src/baselines`, and `src/experiments` (prepare/train/evaluate wiring; `historical/` holds retained probes). Entry points are `src/train.py`, `src/evaluate.py`, and `hpc/run.sh`; the config is `configs/geneeffect_joint.yaml`. There is no `aivc_model` package; retired code lives in gitignored `archive/` and Git history.
Check terminology against docs/01-blueprint.md sections 6-7 (what the metrics measure; current scientific state) and call out conflicts or stale wording.
Return concise evidence with file paths and symbols; do not edit files.
"""
