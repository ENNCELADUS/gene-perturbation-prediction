AIVC-0408

目前优先要做的工作可以归纳为这几条：
1. 明确任务定义：把项目正式收敛到 cancer-specific target ranking / essential gene + SL（合成致死） discovery，而不是泛泛的 perturbation prediction。
2. 梳理数据路线：系统盘点并对齐 DepMap、TCGA、cancer Perturb-seq 三类数据的样本、基因、context 和标签。
3. 定义 ground truth：确定 essentiality score、SL label、viability/depletion readout 分别怎么进入训练与评估。
4. 处理 context：优先挑选有明确 biomarker / mutation context 的癌种或亚型。
5. 保留上学期 scGPT + ranking head 和 linear baseline。
6. 调研 backbone：评估 Tahoe-x1 / Stack等新模型 是否适合当前问题。


润色版 transcript
A： 我在想，要不要围绕 cancer cell 的 Synthetic Lethality (SL) 开一个项目。整体上，这个方向和我们上学期做的工作在任务建模上其实比较接近，主要区别是把数据和应用场景切到 cancer target discovery 这边。
B： 可以，这个方向是合理的。关键问题不是模型能不能搭，而是 data 怎么选。你现在最需要想清楚的是：这个问题有没有足够可靠的 ground truth。如果目标是从 transcriptomic state 反推 cancer-specific target，那么数据里必须有能支撑靶点优劣判断的标签。
A： 我现在看到比较靠谱的一个资源是 DepMap。它至少在 essential gene 层面有比较强的参考价值。如果能进一步结合 SL pair 或者 cancer-specific dependency，那就更理想。
B： 对，DepMap 很重要，因为它能提供相对可靠的功能性 readout。对你们这个问题来说，最核心的是：模型最后要挑出来的，不只是“和表达变化相关”的基因，而是对癌细胞真正有杀伤力、或者至少有较强 dependency 的基因。所以如果能把 DepMap essentiality、SL 证据，甚至病人层面的 TCGA 信息结合起来，整体叙事会更完整。
A： 对，我也在想是不是可以把 cell line-level 的 DepMap，和 patient-level 的 TCGA 结合起来看。因为它们看到的是两个不同层面：一个是功能依赖，另一个是临床背景和 mutation context。
B： 这是一个很好的思路。DepMap 提供的是 cell line 层面的 functional dependency，TCGA 提供的是 patient-level 的 mutation、expression 和临床 context。两者如果能打通，你们就不只是做一个 ranking model，而是在做一个更接近 translational research 的 pipeline。

A： 另外一条线是 Perturb-seq。上学期我们已经接触过一些 Perturb-seq 数据，比如 Norman 那类数据。它的好处是可以直接看到 perturbation 后的 transcriptomic response，而且有单基因和双基因 perturbation，可以直接做 reverse ranking。
B： 对，Perturb-seq 是你们现在这条线里非常关键的数据类型。因为你们做的其实不是单纯的 forward prediction，而是想从 expression signature 反推 candidate target。这个任务本身就和 reverse perturbation identification 很接近。
A： 我们上学期其实已经尝试过：用 scGPT 做 encoder backbone，再接一个 ranking head，把 candidate perturbed genes 排序。结果是，传统方法比如 KNN 在头部排序上反而更强，尤其是 Top-10；但如果看更大的候选集合，比如 Top-50，scGPT 这种 pretrained model 的 recall 会更好。
B： 这个现象很有研究价值。它说明简单的 geometric method 在强信号样本上确实有效，但 foundation model 在更复杂、更弱信号、或者多靶点场景下，可能更擅长做 broader recall。这个结论本身就可以成为论文里的一个亮点，不一定非得把故事讲成“深度模型全方位优于 baseline”。
A： 对，所以我现在觉得，后面如果转向 cancer SL，这条 reverse ranking 的技术路线其实还能继续用，只是要把 ranking target 从“最像哪个 perturbation”转成“哪个 target 最值得优先验证”。

B： 但这里有一个生物学上的关键问题，你们必须想清楚。做 SL 或 essential gene screening 时，我们真正关心的是“哪些 perturbation 能让细胞死亡”，也就是 viability 或 killing effect。可问题在于，很多 Perturb-seq 或 transcriptomic measurement 看到的，往往是活下来的细胞，而不是已经死掉的细胞。
A： 对，这正是我最担心的地方。我们看到的是 surviving cells 的 transcriptome，但真正关心的是 dying cells 或 dead cells。也就是说，观测和目标之间其实存在一种 counterfactual gap。
B： 没错。这几乎就是一个 counterfactual inference 问题。你观测到的是“活着之后的状态”，但你想知道的是“如果继续发展，这些细胞会不会死、为什么死、是否因为这个 perturbation 而死”。中间有明显的 observation bias。这个矛盾如果不说清楚，后面从 transcriptome 直接跳到 cell death，会显得逻辑链条不够严密。
A： 所以我在想，短期内是不是不要把问题定得太细，不一定非要追踪 single-cell level 的死亡过程。我们可以先退一步，只做 population-level 的 readout，比如把 essential gene screening 或 SL screening 的结果，作为一个量化标签来训练模型，让模型学会识别“哪些 target 更可能有效”。
B： 这个我同意。先不要一上来就要求模型解释“细胞具体怎么死”。那太难，而且目前数据也不够。更现实的做法是：先利用已有的 screening label，比如 viability、depletion、essentiality score、SL score，训练一个有生物学意义的 ranking system。先把“target prioritization”做出来，再考虑更深层的机制建模。

A： 还有一个相关问题：现在最主流的 CRISPR screening，包括 essential gene screening 和 SL screening，很多时候本质上还是通过 RNA abundance、gRNA abundance 或 cell count 的变化来间接推断效应。这里面肯定有偏差。比如中途已经死掉的细胞，往往是测不到的。
B： 对，所以你们后面要在文章里明确讲这个 limitation：现有 readout 并不直接等价于真实死亡过程，而是某种 proxy。理想情况下，如果能结合 cell painting、imaging、甚至 lineage tracing，当然更好，但那可能已经超出现在这个项目的可执行范围了。
A： 明白。所以当前阶段，最合理的 framing 可能是：我们不是直接预测 single-cell death trajectory，而是结合 Perturb-seq transcriptome、DepMap essentiality、SL label、以及 TCGA context，做一个 cancer-specific target ranking framework。
B： 对，这样 framing 就顺了。你们可以把 Perturb-seq 作为 transcriptomic mechanism 层，DepMap 作为 functional dependency 层，TCGA 作为 patient context 层。最后形成一个相对完整的闭环：expression state -> candidate target ranking -> functional evidence -> disease context validation。

A： 另外，我还看了一些比较新的 virtual cell / perturbation model，比如基于 Tahoe-100M 的模型，还有 Tahoe-x1 这类路线。我不太确定它们对 cancer SL 这个问题到底有多大帮助。
B： 可以看，但不要为了追新模型而追新模型。你们现在更重要的是根据问题来选 architecture。scGPT 作为 encoder 可以继续试，因为它对 transcriptome representation 确实有帮助；但如果新的模型在 cancer perturbation data 上更强，或者对 context generalization 更好，也可以纳入比较。重点不是模型名字，而是它能不能解决你们手上的生物学问题。
A： 对，我现在的想法也是，先把 scGPT 作为 baseline encoder 保留，再调研 Tahoe-x1 这种模型是否更适合 cancer perturbation / drug perturbation 场景。
B： 可以。你们接下来一周的重点，应该是把数据路线先定下来，而不是先纠结 model engineering。先确认：第一，有没有合适的 cancer Perturb-seq dataset；第二，能不能接上 DepMap essential gene / SL 标签；第三，TCGA 能在多大程度上提供 mutation-specific 或 biomarker-specific context。

A： 对，尤其是 TCGA 这块我觉得不能丢。因为单纯 cell line-level 的 dependency 和 patient-level 的临床异质性还是不一样。TCGA 至少可以帮助我们定义某些 biomarker-specific 背景，比如 BRCA loss 这样的 context。
B： 这点很关键。经典 SL 场景本来就不是简单的“双敲两个基因”，而常常是“一个已存在的 cancer-specific alteration + 一个可干预靶点”。也就是说，一个是 biomarker / mutation context，另一个才是你真正要打的 target。如果你们能把这个 logic 讲清楚，就会比泛泛谈双基因组合更贴近真实临床。
A： 明白。也就是说，最经典的 SL 设计其实是：在带有某个特定 mutation 或 loss-of-function background 的肿瘤亚群里，寻找最优 target。
B： 对。所以你们后面做数据筛选时，不只是看有没有 perturbation 数据，还要看有没有足够明确的 context marker。如果能把 BRCA-like、KRAS-mutant、或者其他明确 biomarker context 的样本拎出来，那你们的故事会更像真正的 precision oncology。

A： 总结一下，当前项目最现实的方向应该是：一，基于上学期已有的 reverse ranking 框架继续推进；二，把问题正式转向 essential gene + SL；三，优先打通 Perturb-seq / DepMap / TCGA 这三类数据；四，模型上先保留 scGPT，同时调研 Tahoe-x1 或其他更适合 cancer perturbation 的 backbone。
B： 对，我觉得这就是今天讨论的核心结论。具体往下推进时，先不要把问题定得过满。短期目标是先做出一个可信的 target ranking 结果；中期再考虑如何把 transcriptomic change 更好地映射到 viability / cell death；长期再去碰真正的机制解释或者更细粒度的 counterfactual modeling。
A： 好，那我接下来就按这个方向，把课程项目描述也改掉，明确写成围绕 essential gene 和 Synthetic Lethality (SL) 的问题设定。
B： 可以。把数据、目标、预期输出、以及参考 baseline 都写清楚。尤其要把 DepMap、TCGA、Perturb-seq 这些数据源的角色写明白。这样不只是一个课程项目题目，也会更像一个可以继续推进的 research question。
