# Joint GeneEffect: batch 1024 deployment

On 2026-09-06 the user explicitly selected dependency batch **1024 per rank**
after the [vectorization probes](2026-09-06-joint-vectorization-diagnosis.md).
Configuration and training design were updated at `36acbd0` and synchronized
through Git to `apg3op6hp3v99-0`, `/2023533015/VCC_Project` (SSH port 30030).
Two H20 workers now process **2048 dependency rows per optimizer update**.
Response replay remains 64 conditions/rank every four updates. Learning rates,
loss definitions, seeds and validation/checkpoint selection are unchanged.

## Explicit checkpoint transition

The source run is `joint_seed0_20260906T174004Z_vector`. Its last complete
checkpoint has `next_epoch=2`, `global_step=11778`, `best_epoch=1` and
`best_loss=0.01632782630622387`. That validation value predates this batch change.
The source stopped at step 13852; its 2074 uncheckpointed epoch-2 updates are
excluded from the new run. The original files and logs remain preserved.

New run: **`joint_seed0_20260906T174818Z_b1024`**. Derived `last.pt` and `best.pt`
declare the new continuation configuration and retain the original configuration,
checkpoint SHA-256 and changed field in `batch_size_transition` metadata. All
original payload fields other than `config` were recursively compared after
reloading the derived files, including exact tensor equality for model weights,
optimizer, normalization/projection, preprocessing and per-rank RNG state.
`run.json` and `batch_size_transition.json` retain the transition provenance.

This is an explicit batch transition, not a claim that the source was trained
with batch 1024 or that the next update is numerically identical. Larger batches
change the number of updates per epoch and response replay exposure per dependency
row. Previously completed metrics are inherited with their original row counts;
new training logs report 2048 dependency rows/update.

## Deployment verification

The 13 configuration/launcher tests passed. The ordinary resume config-equality
guard remains active; the supplied config matches the declared derived checkpoint
config. Launcher 70478 and workers 70610/70611 were stopped within the authorized
scope. The new run uses `hpc/run.sh train`, supervisor 71023, launcher 71024 and
workers 71156/71157.
Actual ordinary and replay updates have been observed on both H20s without OOM.

After at least 32 warmup updates, three consecutive 15-second windows measured
**2866.93 dependency rows/s**, a **28.6% increase** over the previous vectorized
batch-256 production run (2229.57 rows/s). Every window completed 21 optimizer
updates, covering epoch-2 steps 11836--11899.

| Production measure | Batch 256/rank | Batch 1024/rank |
| --- | ---: | ---: |
| Dependency rows/s | 2230 | **2867** |
| GPU 0 / 1 mean utilization | 75.51% / 77.33% | **88.09% / 90.09%** |
| GPU 0 / 1 memory MiB | 18166 / 18164--18166 | **62300--62302 / 62294--62296** |

There are 45 telemetry samples/device, with stable memory at about **60.8 GiB
per card**. This is observed GPU memory occupancy, including allocator reserve,
not the single-process peak allocated tensor memory from the earlier probe.
The sampler exited after the bounded measurement. These short steady-training
windows do not establish full-epoch runtime or eventual scientific quality.
The [production evidence](2026-09-06-joint-batch1024-production.json) includes
exact timestamps, checkpoint hashes, state-equality verification and launch
provenance. Full original configurations remain in the remote transition record.

Remote artifacts:

- `outputs/geneeffect_joint/joint_seed0_20260906T174818Z_b1024/`
- `outputs/launches/joint_seed0_20260906T174818Z_b1024/`

Training continues in epoch 2. Launch and throughput evidence do not establish
terminal training completion or a new validation result.
